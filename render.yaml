services:
  - type: web
    name: zelcry
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn zelcry.wsgi"
    postDeployCommand: "python manage.py migrate"
